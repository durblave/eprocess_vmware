---
- name: 5.2 - RHEL - ping host
  hosts: all #"{{ search_hosts }}"
  gather_facts: true

  # vars:
  #   search_hosts:
  #   find_patterns_list

  tasks:
    - name: Check reachability
      ping:
      register: reachability

